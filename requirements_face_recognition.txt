# Instalar DEPOIS do requirements.txt e obrigatoriamente com --no-deps:
#   pip install --no-deps -r requirements_face_recognition.txt
# Motivo no cabeçalho do requirements.txt: a dependência "dlib" é atendida pelo dlib-bin.
face-recognition==1.3.0
